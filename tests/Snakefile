configfile:
    'config.yaml'

include:
    '../single-cell.sm'
    
rule test_filter:
    input:
        expand(rules.fastp_10x.output, sample=SAMPLES)

rule test_cellranger:
    input:
        expand(join('data', 'processed', 'singlecell', 'quant', '{quant}', 'scanpy', '{sample}', '{sample}.h5ad'), sample=SAMPLES, quant=['cellranger', 'alevin', 'umitools']),
        #expand(join('data', 'processed', 'singlecell', 'quant', '{quant}', 'seurat', '{sample}', '{sample}.rds'), sample=SAMPLES, quant=['cellranger', 'alevin', 'umitools'])
