#-*- mode:snakemake -*-
extra_conf_fn = srcdir('gcfdb.config')
if os.path.exists(extra_conf_fn):
    with open(extra_conf_fn) as fh:
        c  = yaml.load(fh) or {}
        update_config2(config, c)

include:
    'gcfdb/reference_db.db'
include:
    'gcfdb/10xgenomics.db'

